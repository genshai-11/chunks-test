export type NarrationVariantState = { target: 'section_intro' | 'test_item'; itemId?: string | null; language: 'vi' | 'en'; voiceId: string; approvalStatus: string; sourceTextHash: string; currentSourceTextHash: string; audioAssetId: string | null }
export type NarrationReadinessSummary = { introApproved: boolean; approvedItems: number; expectedItems: number; stale: number; ready: boolean }
export function narrationReadiness(variants: NarrationVariantState[], expectedItemIds: string[], language: 'vi'|'en', voiceId: string): NarrationReadinessSummary { const scoped=variants.filter(v=>v.language===language&&v.voiceId===voiceId); const current=scoped.filter(v=>v.approvalStatus==='approved'&&v.audioAssetId&&v.sourceTextHash===v.currentSourceTextHash); const stale=scoped.filter(v=>v.approvalStatus==='approved'&&v.sourceTextHash!==v.currentSourceTextHash).length; const introApproved=current.some(v=>v.target==='section_intro'); const approved=new Set(current.filter(v=>v.target==='test_item'&&v.itemId).map(v=>v.itemId)); const approvedItems=expectedItemIds.filter(id=>approved.has(id)).length; return {introApproved,approvedItems,expectedItems:expectedItemIds.length,stale,ready:introApproved&&approvedItems===expectedItemIds.length&&expectedItemIds.length>0&&stale===0} }
